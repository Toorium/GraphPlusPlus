schema_version = "1.0.0"

id = "graph_plus_plus"
version = "0.1.0"
name = "Graph++"
tagline = "Smart graph editor & editable motion path. Drag the path, fix arcs, apply easing — without leaving the 3D view."
maintainer = "Toorium"
type = "add-on"

# Optional: link to website / repo
website = "https://github.com/Toorium/GraphPlusPlus"

# Supported Blender versions
blender_version_min = "4.2.0"

# Optional add-on section — license required for listing on extensions.blender.org
license = [
  "SPDX:GPL-3.0-or-later",
]

# Optional: tags for discoverability
tags = ["Animation", "3D View", "Graph Editor"]

# Permissions
permissions = {
  files = "Required for the GitHub auto-updater to download and apply new versions.",
  network = "Required to check GitHub for Graph++ updates.",
}

# Optional: optional Python dependencies (none — algorithmic only)
